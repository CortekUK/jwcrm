"use client";

import MyLeavePage from "@/app/my/leave/page";

// Renders the leave self-service inside the unified dashboard shell (sidebar,
// role switcher) so staff can find and submit their own leave requests.
export default function AdminMyLeavePage() {
  return <MyLeavePage />;
}
